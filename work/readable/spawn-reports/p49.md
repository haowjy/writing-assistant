# Create the first 24 evaluation cases

Background agent p49 was asked to create the first batch of evaluation tasks and verify that the repository could compile and load them. It delivered 24 cases marked `final_eval`, the role for final evaluation, with their own source catalog; the reported balance and structural checks passed.

The cases are in [gen-wave1-eval](../../../data/processed/gen-wave1-eval/manifest.json). A source catalog records the material each task uses and its provenance, meaning where that material came from. Keeping that catalog with this batch makes its source references self-contained.

Every requested balance target matched, including six cases with 8–12 follow-up messages. Verification covered 30 artifact selectors, which locate the reply or file text to be scored; 161 required checks; and all six retrieval paths, the sequences of tool actions that expose the needed source facts. Compilation turns authored scenarios into the repository's task packages; loading confirms those packages can be read. These checks establish task preparation, not a model's performance on the tasks.

The agent reported no blockers and made no paid API calls, training runs or evaluation runs. The [full report](../../training-data/wave1-eval.md) records files created, source-provenance decisions, target-versus-actual counts, exact validation commands and observed outputs. It also records an initial validator-fixture failure and its correction. That report's original location was `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/data-generation/wave1-eval.md`.
