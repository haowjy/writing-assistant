# Report

The report is written and verified.

## Report

**Deliverable:** `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/transcript-analysis/tool-use.md` — 313 lines, 18,434 bytes, non-empty. No files under the repo (`src/`, `tests/`, `runs/`) were modified.

**What was done**

1. **Inventory over all 100 attempts** (50 nf4 + 50 bf16), built from `trace.jsonl` `tool` records and `result.json`:
   - Tools offered in exactly **25/50 attempts per arm** (F2, F4, and the 5 F5 `retrieved_kb` cases). The other 25 per arm had `visible.json["tools"] == []` and no workspace.
   - **50/50 tool-offered attempts called ≥1 tool**; total calls **158** (nf4 73, bf16 85) — `read_file` 69, `write_file` 73, `patch_file` 11, `list_dir` 5, **`search` 0**.
   - `tool_errors`: **2/158 calls** (1 per arm, both `read_file("kb/")` on a directory in F5-07); **1 abort** (nf4 F4-02, malformed tool-call JSON → empty output).

2. **Read-before-write ordering:** reconstructed per attempt — **0 cold writes** (0/43 writing attempts wrote before reading). 61 writes created new files; of 23 writes to pre-existing files, 10 had a same-path read and 13 targeted files the model itself had created earlier in the same attempt.

3. **Failures, with quotes beside contradicted sources:**
   - **nf4 F2-02** fabricated a save ("saved to `drafts/scene.md`") with only a `read_file` in the trace and no workspace file (1/100).
   - **10 of 30 nf4 continuity checks failed**; I quoted the actual prose and the contradicted `visible.json`/`kb`/`source` fact for each (e.g. nf4 F1-06 "like someone clearing his throat" vs "heard their own voices"; nf4 F5-08 "They agreed that entering alone was dangerous" vs "nobody agrees"; nf4 F5-09 Pia holding the collector's key after reading `kb/canon.md`). 8/10 were in no-tool scenarios, 2/10 in tool-offered attempts.
   - **Canon authorization:** 1/10 F4 `update` attempts (bf16 F4-08) replaced committed canon ("Noor … retaining her existing residence papers") with the draft-only suggestion unlabelled; 9/10 respected or labelled it.

4. **Arm/specificity comparison:** identical tool-use behaviour across arms; loose briefs did **not** get less reading (mean calls loose ≥ explicit in both arms; the raw 13 vs 12 split is a scenario-assignment artifact).

**Issues/blockers:** none. Key limitation recorded in the report: the Q13 continuity check exists only for the graded nf4 arm and only F1/F2/F5 (30 checks); bf16 is ungraded, so the hallucination table is a verified lower bound and I hand-checked bf16 separately. An earlier agent's scratch files exist in the same spawn work dir (`all_texts.md`, etc.); I did not use or alter them.
