# Report

## Run report

**Deliverable:** `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/transcript-analysis/failures.md` — 23,414 bytes, 196 lines, verified non-empty.

### What was done
Read all 100 `custom50` attempts: 49 nf4 `card.json` (one errored), 50 bf16 `card.json`, 50 nf4 astra `scorecard.json`, plus `result.json` messages/tool traces and workspace files. Built the per-check pass/fail table, traced every failing check to its judge `rationale`/`evidence` and to the raw transcript/tool call, and classified each failure mode capability vs harness/contract.

### Key findings (all with counted denominators + quoted transcript text)
- **Coverage:** 99 completed + 1 error (F4-02 `ValueError` from the tool-call dialect parser). 93/100 attempts produced a deliverable; 7 prose attempts had no extractable `scene` (nf4 5/30, bf16 2/30). bf16 has **115 pending semantic checks and no scorecards** — not comparable on semantics.
- **Capability failures (trainable):** word-budget overlength **16/23 scored** (nf4 9/15, bf16 7/13; all 224–293 vs cap 220); continuity **10/30 nf4**; pov-style register **9/15 nf4**; wiki-links **10/10** explicit F4 across both arms; retrieval `evidence_exposed` **4/10 state, 5/10 belief**; F4 required-fact omissions; F2-02 saved-scene; F3-02 three-directions.
- **Harness/contract bugs:** `optional-context` judge fails omissions its own prompt says to accept (**5/10 nf4**) plus F4-07 `consequential-state`; `pov-style` conflates POV+style (9/9 failures passed the POV half); `wiki-only` rejects a defensible non-`kb/` help file (1/10); untouched files vacuously pass `saved-scene`/`preserve-ending`/`edit-scope` (~7/60 prose attempts); F4-02 protocol parse crash.

### Verification
- File created via `write`, confirmed with `ls`/`wc`/`head`.
- Every taxonomy entry cites a denominator and a verbatim quote (model output, tool trace, or judge evidence).
- Repo tree untouched; scratch files removed; no network/API/training calls.

### Issues
- **bf16 semantic failures are unknowable** (ungraded) — reported as ungraded rather than inferred. The `pov-style`/`continuity` judgements are LLM-based and subjective; flagged strictness on F1-06/F1-09. Distribution runs were out of scope for this lane.
