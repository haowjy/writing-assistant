# Report

# Run Report — Steer Scripts

## Deliverables (both exist, non-empty, verified)

- `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/data-generation/steer-scripts.json` — 37,169 bytes, parses as JSON, `schema_version: 1`.
- `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/data-generation/steer-scripts.md` — 3,252 bytes coverage companion.
- (Helper, not a required deliverable) `build_steer_scripts.py` in the same work dir — assembles the JSON and generates the markdown table from computed counts, with assertions that fail the build on any constraint violation.

## What was authored

**36 scripts / 210 steers**, 12 scripts per depth band, all generic over worlds using `{protagonist}`, `{other}`, `{object}`, `{place}`. Schema per script: `id, group, depth_band, n_steers, response_pressure, rationale, steers[]`, plus `tests` (assumption scripts) and `escalates_conflict` where applicable. Every steer is `{text, kind}`.

- **Depth bands:** 2-3 (12 scripts, 27 steers), 4-6 (12 scripts, 52 steers), 8-12 (12 scripts, 131 steers).
- **Coverage table (band × kind)** shows all 9 kinds present in *every* band; overall counts: ADD_CONSTRAINT 46, PUSH_BACK 29, APPROVE 29, REQUEST_REVISION 26, CHANGE_DIRECTION 22, NEW_MATERIAL 19, CLARIFY_ANSWER 14, SCOPE_CUT 14, INTERRUPT 11.
- **Response pressure** per script with reason: PERMITS_DEFAULT 15, MUST_NOT_ASK 12, REQUIRES_ASK 9.
- **10 noticed-a-silent-assumption scripts** (`group: silent_assumption`), distributed across bands (3× 2-3, 3× 4-6, 4× 8-12), each with a `tests` field describing the silently-assumed thing: stranger-vs-acquaintance, unrequested romance, wrong character action, unreliable narrator, night-vs-day, off-screen death, abandoned place, improper secret knowledge, unauthorized season, invented backstory death.
- **Terse (<12 words):** 191/210 (90%) — requirement was ≥25%.
- **Escalating conflict (no fast resolution):** 6 scripts — SS-A08, SS-B04, SS-B12, SS-C05, SS-C06, SS-C09 (requirement ≥6).

## Verification performed

- JSON parses; `schema_version == 1`.
- Every steer has a `kind` drawn from the 9 allowed kinds.
- Every script names `response_pressure` (one of 3) with a non-empty `rationale`.
- `n_steers` matches `len(steers)`; band ranges enforced (2-3, 4-6, 8-12).
- All 9 kinds appear in each depth band, and all 3 bands are populated.
- Both output files `-s` (non-empty), sizes confirmed.
- Grepped for literal world names (`Mara`, `Ilan`, `tidal`, `ledger`, etc.) — none present (only a false hit on "h**aven**'t"); placeholders only.

## Notes / deviations

- I did not modify `src/`, `tests/`, `data/`, or `runs/`; all writes are under the p34 work dir.
- No API calls, training, or evaluation runs.
- Band counts are equal by **script** (12/12/12); the 8-12 band necessarily carries more steers, so its kind totals dominate the ALL row. If "equal coverage" was meant as equal *steer* volume, that could be rebalanced, but equal scripts is the natural reading.
- 90% terse over-delivers on the ≥25% rule; the mix includes one-word replies ("Good.", "Both.") up to ~16-word messages. No ambiguity in the three response-pressure categories depends on model-private reasoning — each pressure derives from the surface text.
