# Report

The specification is written and verified.

## Report

**Deliverable:** `/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/data-generation/balance-spec.md` — 33,683 bytes, 463 lines, non-empty.

**What I did**
1. Read the project docs and schemas: `AGENTS.md`, `src/.context/CONTEXT.md`, `variation-catalog-v1.json`, `task-generation-v1.json`, `worlds.json`, `development.json`, `specificity.py`, `longform_suite.py`, `longform-v1.json`, `sft-v1.jsonl`.
2. Read the design/reward docs that already frame the axis problem: `training-distribution-axes.md`, `rl-task-generation.md`, `rl-algorithm-decision.md`, `custom-eval-suite/coverage.md`, `data.md`, SFT `plan.md`, `dataset-starter.md`, `task-generator-instructions.md`.
3. Measured the current pools directly (ratios, confounds, coverage) rather than trusting the prompt numbers — and found the `100 prepared requests / 54 accepted` reality behind the "54 tasks" claim.
4. Wrote the hierarchy-based balance spec.

**Key decisions / content**
- **Hierarchy:** 13 HARD axes, 14 COVERAGE, 6 SAMPLED, 9 REPORTED — every axis in exactly one ring with a rationale. The 10-authoring-rule `composition_rules[0]` is interpreted as a task-level ingredient cap, which justifies COVERAGE/SAMPLED rather than a Cartesian product.
- **Targets:** `TRAIN : VAL : TEST = 300 : 60 : 60 (5:1:1)`, defended via the GRPO distinct-prompt floor, 1/√rollouts variance argument, and source-group disjointness. Test internal 60 + the 6 frozen `longform-v1` + 12 EQ-Bench external. Full marginal tables per split.
- **SFT vs GRPO:** explicitly separated as one prompt pool with two admission filters — SFT ≈ 200 accepted trajectories (coverage-driven); GRPO needs ~256–300 distinct prompts (reuse ≤4). Name the reward-reading axes (word budget, continuity/retrieval, wiki-links, channel/tool, ask-modality) that become load-bearing for the held-out suite, and state that the external anchor is the only falsifier.
- **Separation:** lineage-group rules, disjoint val/test, hash-based holdout via `holdout_audit`, and how EQ-Bench differs from our own suite.
- **Test-vs-train asymmetry** section and **13 concrete HARD assertions** (plus the channel-confound assertion).

**Contradictions flagged in the spec**
- `CONTEXT.md` says loose prompts govern *viewpoint*, but `specificity.py DECISION_POINTS` has no `viewpoint`/`idea_count`/`ending` point.
- The saved `requests.json` is pre-generation schema v2 (`generated_tasks: 0`) while `generated/manifest.json` records 100 requested / 54 accepted.
- `channel_free`, deep `stages≥4`, and `emotional_register`/`pov` vocabulary do not exist in the current schema, so H5/H6/H9/H10/C6/C7 require proposed (not applied) schema additions.

**Constraints honored:** no edits to `src/`, `tests/`, `data/`, `runs/`; no API calls, training, or evaluation runs. Only the target directory was created and the spec written.

**Issues/blockers:** none for the deliverable. The spec itself records that H11/H13/C13 are gated on acquiring more than the current 5 source works / 2 lineage groups, and that `holdout_audit` must fail closed until then.
