# Report

Report saved and verified.

## What was done

Wrote the full Lane 4 analysis to the required absolute path:

`/home/jimyao/.meridian/projects/orange-juniper-leaf/spawns/p34/work/transcript-analysis/specificity.md`
(19,296 bytes, 344 lines, non-empty, confirmed on disk)

Read `visible.json`, `result.json` (`output`, `turns`, `tool_calls`, `trace`, `before`/`after`) and `workspace/` for all 50 attempts in each `custom50` arm (nf4 + bf16), plus the two 25-repeat `distribution` arms as a control. No files under `src/`, `tests/`, or `runs/` were modified.

## Key findings in the report

- **Real contrast:** explicit briefs state a 120–220 word target in 20/25 and POV in 15/25; loose briefs state each in **0/25**. Loose still names the deliverable in 12/25, so the axis is length/POV/scope, not files. Flagged the unclean bits: mismatched condition mixes per level (F2/F3/F4/F5), and 12/25 "loose" briefs that name paths.
- **Behaviour by level (both arms):** loose deliverables are ~1.7× longer (bf16 399 vs 240 mean words; nf4 487 vs 288). Turns, tool calls and tool errors are near-flat; the single tool error in each arm is the same loose scenario F5-07 (`read_file("kb/")` → "Is a directory"). Final-answer rate 25/25 everywhere except nf4 F4-02 (harness JSON error).
- **Classification — every final-answer attempt covered (99 = 49 nf4 + 50 bf16):** ASK 7, SAY 3, SILENT 89, IGNORE 0. Across all 40 non-F3 loose attempts the model asked **0/40**, stated a default **0/40**, silently chose **40/40**. The only surfacing is F3 (the brief is itself "what directions could I try"). Quoted examples include the model internally weighing — then declining — asking (bf16 F5-09, nf4 F2-09/F5-07).
- **Stated constraints:** 120–220 words honoured in only 10/30 (nf4 3/15, bf16 7/15), almost always overshoot; F2 `local_revision` file/tag format honoured 1/6 (nf4 left the file untouched in all 3); F4 KB ≤500 words 6/6 and draft-only/accepted-revision distinction honoured 9/9.
- **Control:** on F1-01 explicit, bf16 hit 25/25 in-range while nf4 hit 7/25, showing part of the explicit length violation is the nf4 quantisation rather than the specificity axis.

Scratch artefacts (attempt-level flattening, dumps, artifact metrics) were kept under `$MERIDIAN_ACTIVE_WORK_DIR`, not the repo.
